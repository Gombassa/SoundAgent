anthropic
requests
python-dotenv
pyyaml
watchdog
ffmpeg-python
mutagen
wsgidav
aiofiles
tenacity
fastapi
uvicorn[standard]
jinja2
python-multipart
alembic
soundfile
scipy
numpy

# ── Duplicate detection — external binary, not a pip package ─────────────────
# fpcalc (Chromaprint) — download from https://acoustid.org/chromaprint
# Place at tools/fpcalc.exe (Windows) or set fpcalc_path in config.yaml

librosa>=0.10.0

# ── P7 audio analysis — optional ML dependencies ─────────────────────────────
# Install via scripts/install_audio_deps.bat or manually:
# tensorflow
# tensorflow-hub
# openai-whisper
# torch
# torchaudio
# AudioCLIP  →  git clone https://github.com/AndreyGuzhov/AudioCLIP && pip install -e ./AudioCLIP
# essentia   →  pip install essentia  (Linux/macOS; Windows: essentia-tensorflow wheel)
